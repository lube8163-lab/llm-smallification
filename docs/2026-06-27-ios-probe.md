# 2026-06-27 iOS Probe Setup

Status:

- Built llama.cpp XCFramework successfully at `external/llama.cpp/build-apple/llama.xcframework`.
- Size of the XCFramework: about `745M`.
- Applied a Gemma 4 12B probe patch to `examples/llama.swiftui`.
- Verified a generic iOS Release build with signing disabled.
- Verified `scripts/build_ios_probe.sh` after patching.

Build command used:

```bash
xcodebuild \
  -project external/llama.cpp/examples/llama.swiftui/llama.swiftui.xcodeproj \
  -scheme llama.swiftui \
  -sdk iphoneos \
  -configuration Release \
  -destination 'generic/platform=iOS' \
  CODE_SIGNING_ALLOWED=NO \
  CODE_SIGNING_REQUIRED=NO \
  CODE_SIGN_IDENTITY= \
  PRODUCT_BUNDLE_IDENTIFIER=com.tasuku.Gemma12BProbe \
  build
```

Repro command:

```bash
./scripts/build_ios_probe.sh
```

Probe settings added to the Swift wrapper:

- `n_gpu_layers = 1`
- `use_mmap = true`
- `use_mlock = false`
- `n_ctx = 512`
- `n_batch = 16`
- `n_ubatch = 16`
- `type_k = GGML_TYPE_Q4_0`
- `type_v = GGML_TYPE_Q4_0`
- `offload_kqv = false`
- `op_offload = false`
- output cap `n_len = 16`

These mirror the only local Mac configuration that generated text successfully.

The app now lists:

- `Gemma 4 12B QAT (Q4_0, 6.5 GiB)`

Next device-side task:

1. Open `external/llama.cpp/examples/llama.swiftui/llama.swiftui.xcodeproj`.
2. Select a real 8 GB iPhone.
3. Use a personal team/bundle id for signing.
4. Install the app.
5. Download or import `gemma-4-12b-it-qat-q4_0.gguf`.
6. Load it and watch Xcode console for `Gemma12BProbe settings`.
7. Try a tiny prompt such as `Hi`.
